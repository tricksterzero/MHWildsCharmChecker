
# コード精査ログ（詳細アーカイブ）

[review-ledger.md](review-ledger.md)の各ファイル行に対応する精査履歴の詳細版。台帳側は最新状態の要約（状態列＋相互作用チェック相手・未解決の既知課題を1行）のみを残し、精査のたびの詳細（発見内容・修正過程・実測値・Codexとの相談内容等）はここに時系列で追記する。原文をそのまま残す（要約せず、後から経緯を追えるようにする）。セクション構成・ファイル並び順はreview-ledger.mdに合わせる。

2026-08-03、[コード精査台帳テンプレート](../../Knowledge/Tech/ClaudeCode/code-review-ledger-template.md)に基づき、旧`review-ledger.md`（テーブルの1セルに全履歴を詰め込む単一ファイル構成）から本ファイルを分離した。以下の内容は分離前の台帳の各セルをそのまま移設したものであり、加筆・要約はしていない。

## 高優先（コアロジック・複雑な画像処理・ビジネスルール）

### SlotIconAnalyzer.cs

状態: 済(2026-07-21、Codex相談、実バグ2件修正+観察点2件)

SlotIconTypes(定数)・SlotValidation(判定結果の検証)・SkillReadingPipeline(護石名ベース種別判定の受け渡し)。**2026-07-21の再精査(Codex相談)**: `TryRecoverFusedFrame`(融合フレーム回復)の2026-07-21新規追加分を精査。実データ36画像・72領域を再解析したところ、幅超過の「融合候補」自体は24個・16領域に存在し(既知の対象2件だけではない)、トリガー候補自体は珍しくないと判明。**実バグ発見・修正(1)**: 内包パターンの判定(`f.X>=oversize.X && f.Right<=oversize.Right`)がX範囲のみでY範囲(行)の重なりを見ておらず、隣接パターンには課していた`sameRow`条件が無かったため、別行の無関係な大型輪郭を内包パターンと誤認しうる欠陥があった。隣接パターンと同じ`sameRow`条件を追加。**実バグ発見・修正(2)**: 回復した枠を`outResult`に無条件追加しており、(a)1つの内包塊のX範囲内に確定枠が複数ある場合に最初の1件だけを基準にする、(b)複数の`oversizedCandidates`が同じ枠を回復する、といった状況で同一枠が重複追加されうる欠陥があった。追加前にX範囲の重なりを確認し重複なら追加しないガード(`RangesOverlap`)を追加。いずれも36画像の実データでは現状顕在化していない(既知2件のみ回復が発生)が、UI位置・解像度のわずかな変化で条件成立しうるとCodexが指摘、リスク評価は「中」。回帰テストは具体的な誤検出repro画像が無いため追加せず、既存2件の回帰テスト+全176テスト成功で非破壊を確認。**観察点(据え置き)**: (1)`TryRecoverSide`の複数候補時の最大面積採用(`subWidth<wHi*1.5`)は、`RetrievalModes.External`では通常入れ子の内側輪郭が返らないため「同一アイコンの内側/外側境界」という前提が一般には成立しないとCodexが指摘。既知2画像では候補が常に1件のため未検証。(2)画像境界でクリップされた窓は、クリップ前の`subWidth`で1.5倍判定するため、境界付近で本来救済可能な1アイコン分の候補が偽陰性で棄却されうる(誤ったデータを生成する方向ではなく検出を諦める方向のため実害は限定的)。**低優先の観察点**: `ClassifyLevel`の`peak<=0`ガード(黒一色→Unknown)は正しいが、白一色等の非ゼロ一様画像は`n==1`となりLv1と誤判定されうる(枠検出済み領域のため到達確率は低いとCodexも同意)。既存テスト`ClassifyLevel_UniformCrop_ReturnsUnknown`は黒一色のみ検証しておりテスト名と実態にギャップがあるが、対応は見送り。**過去の変更内容(再掲、参考)**: `TryRecoverFusedFrame`(融合フレーム回復、内包パターン+隣接パターンの2種)を新規追加。実データ2件で検証・blast-radius計測(36画像・72領域中対象2件のみ差分)は実施済みだが、本台帳の精査手順(Codex相談)は未実施。BOX/Detail同数タイの優先順位も`>`→`>=`に変更済み(こちらは前回台帳更新後の別セッションで変更、変更履歴は詳細省略)。**実バグ発見・修正**: `ClassifyLevel`(112行目)で列プロファイルが全て0(`peak==0`)の場合、`threshold`も0になり全列が閾値を満たして`n==1`扱いとなり`Unknown`ではなく`Lv1`と誤判定される穴があった。枠検出済み領域のため実データでは起きにくいが安全側で`peak<=0`を先にUnknownとするガードを追加(回帰テスト`ClassifyLevel_UniformCrop_ReturnsUnknown`追加)。**ドキュメント不整合発見・修正**: クラスXMLコメント「legacy/slot-icon-pipeline/pipeline.pyの移植」は不正確だった。git履歴を裏取りしたところ初回移植コミット`93f76dd`はPoCと完全一致(n==1→Unknown、Lv2判定閾値0.55)だったが、後続コミット`2db3c7f`(2026-06-15)で実データに基づきn==1→Lv1を追加・Lv2判定閾値を0.55→0.50に変更しており、legacy側(参照専用・ビルド対象外)は追随していなかった。コメントを実態(移植+実データチューニング)に合わせて修正。**観察点(据え置き)**: (1)`MergeOverlapping`(184行目)はX近接のみでマージしY座標を無視するため理論上は別行のノイズ候補との誤マージがありうる(後段`FilterYCluster`で大半救済、実データでは未発現)。(2)`MergeOverlapping`の連鎖マージは代表矩形の面積次第で推移的にならないケースがある。(3)`FilterYCluster`(206行目)の単連結クラスタリングは橋渡し候補があれば離れた行を誤結合しうる。3件とも現在の探索領域の狭さと実画像実績から実害小と判断、次回大幅改修時の要再検討事項として記録

**追記(2026-07-27時点、CLAUDE.md記載の後続変更、本台帳側の精査履歴は未反映)**: 融合フレーム回復に(3)同じ行に確定フレームが無い場合のフォールバック（塊の左端・右端からの固定幅窓再探索）と、回復後の再クラスタリング（`FilterYCluster`再実行）を追加。詳細はCLAUDE.mdの「スロットアイコン判定ロジック」節を参照。次回本ファイルを精査する際にこの変更分を対象に含めること。

**2026-08-05の再精査(Codex相談、修正は実データ検証の結果撤回)**: 上記2026-07-24〜27の未反映分（21:9対応・真の21:9ネイティブ対応・装飾品検知の全面再設計）をCodex(`mcp__codex__codex`)と1本ずつではなく、関連する4ファイル(本ファイル・SkillReadingPipeline.cs・MainWindow.xaml.cs・SlotIconTypes.cs)をまとめて1つの意思決定(21:9/装飾品検知まわりの一連の変更)として相談。自分の机上レビューで5点の懸念を洗い出した上でCodexに反例探索を依頼し、Codexは独自に高2件・中3件を指摘。うち本ファイルに関わるのは以下1件。**当初の対応(後で撤回)**: `DetectFrames`(91行目)の`TryRecoverFusedFrame`呼び出し条件が`oversized.Count > 0 && filtered.Count > 0`だったが、`TryRecoverFusedFrame`内のフォールバック(3)のコメントは「同じ行に確定フレームが1つも無い(=護石の唯一のスロットが丸ごと融合した)場合」を明記しており、これは`filtered.Count == 0`のケースを指す。しかし呼び出し条件がこのケース自体を弾いてしまい、コメントが想定する状況の一部には回復ロジックへ到達できていなかった(実装とコメントの意図の不一致)。`TryRecoverFusedFrame`自体は空の`accepted`でも正しく動作する実装であることをコード読解で確認し(全ループが空スキップされ必ずフォールバック(3)の両端探索に到達する)、条件式から`filtered.Count > 0`を削除する1行修正を適用した。**内蔵advisorとの最終レビューで実データ検証の必要性を指摘され撤回**: 2026-07-21の同種変更時に行った「実データ36画像・72領域での回復ON/OFF blast-radius比較」を今回も実施すべきとの指摘を受け、`assets/`全78枚×BOX/DETAIL領域(156領域)で`DetectFrames`の新旧条件差分を測定したところ、意図した`filtered==0`ケース以外に7領域で差分が発生。うち`case1 quest result/20260612192419_1.jpg`のBOX領域で、常時表示される「RARE」ラベル由来とみられる偽陽性の融合塊(oversized)がフォールバック(3)で誤って「回復」され、本来DETAIL領域で正しく読めていた護石(`armor=[0,0,0], weapon=[1,0,0]`)が、`ReadSlots`のBOX優先ロジックにより誤ったBOX側の1件を採用するようになり、結果的に`DecorationEquippedException`で護石全体が除外される回帰を確認(オリジナルコードとの比較で確定)。「同じ行に確定フレームや別行ノイズが一切無い塊」に対する両端探索フォールバックは、手掛かりが無い分だけ精度が低く、BOX優先ロジックと組み合わさると安全側ではなく害のある方向に作用することが判明したため、条件式を`oversized.Count > 0 && filtered.Count > 0`へ復元(=変更なし)。**結論**: `TryRecoverFusedFrame`のコメントが想定する「filtered完全に空」のケースへの対応は据え置きとする。対応するには、フォールバック(3)の両端探索自体の信頼性を上げる(例: 検出候補の形状・位置についてより厳しい事前条件を課す)設計変更が必要で、単純な条件式の変更では安全に実現できないと判断。次回大幅改修時の要再検討事項として記録。**Codexが確認した他の観察点(据え置き)**: `RangesOverlap`(255行目付近)がX範囲のみでY範囲を見ておらず、同じX位置の別行ノイズが先に存在すると真の回復枠が「重複」扱いで誤って棄却されうる。実データでの発現未確認、次回大幅改修時の要再検討事項として記録。

### SlotIconTypes.cs

状態: 済(2026-07-19、Codex相談、0件)

SlotIconAnalyzer(定数の消費)・SlotValidation(ArmorSlotMaxByPositionとの整合)。定数値自体はSlotIconAnalyzer精査時に実データ検証済みのものをそのまま踏襲しており齟齬なし。Codexとの相談でも定数・探索領域・アルゴリズム記述とCLAUDE.mdの一致を再確認、指摘なし

### SlotValidation.cs

状態: 済(2026-07-19、Codex相談、実バグ1件修正)

SlotIconAnalyzer(判定結果)・MainWindow.ClassifyFrames(武器/防具振り分け後に呼ばれる呼び出し元)。ArmorSlotMaxByPosition=[3,1,0](防具3個目は常に0)が実データと矛盾しないか、3フレーム検出される「栄世の護石」(20260614061441_1.jpg、防具2+武器1の特殊護石)で裏取り。ClassifyFramesがhasWeaponSlot時にframes[0]を武器スロットへ先に振り分けるため、SlotValidationへ渡る時点で防具は実質2枠のみとなり制約と整合することを確認(コード確認+実画像目視)。防具側の貪欲法アルゴリズム(降順ソート後、位置ごとの上限に収まれば採用しvalidIdx++)はCodexとの相談で数学的に反例なしと確認(回帰テスト`ArmorSlot1_Lv3Lv2Lv1_RejectsLv2_KeepsFollowingLv1`追加、`[3,2,1]→[3,1,0]`で棄却後に後続Lv1を拾う経路を固定)。**実バグ発見・修正**: 前回(2026-07-12)の観察点(武器スロットはOrderByDescending未実施)を深掘りしたところ、`Validate([0,0,0],[1,1,0])`が`[1,1,0]`をそのまま通す(「武器スロットは実質1個まで」という制約を未検証)ことが判明。現在の呼び出し元は常に`weaponSlots[0]`のみ埋めるため実害はなかったが、将来複数武器スロット護石が実装された場合・呼び出し元にバグが混入した場合の防御として`WeaponSlotCountMax=1`定数を追加し個数上限を強制(回帰テスト`WeaponSlot_MultipleLv1_OnlyFirstKept`追加)。武器側の非ソート自体は現行Lv1限定では実害なし、将来Lv2/Lv3が許可された場合は要再設計(据え置き)

### SkillReadingPipeline.cs

状態: 済(2026-07-21、Codex相談、実バグ3件修正)

ImageVariantFactory・LvParser・SkillNameNormalizer・SkillNameLoader・TextOcrReader・SlotIconAnalyzer(護石名の受け渡し)・CharmTypeLoader。**2026-07-21の再精査(Codex相談)**: `PairByNearestY`(DPペアリング)・`TryRecoverOrphanNames`(孤立1文字名回復)の2026-07-21新規追加分を精査。**実バグ発見・修正(重要度高、DPペアリング自体の構造的欠陥)**: DPのマッチコストに生のY座標差をそのまま使う設計だと、名前-Lv間の行内オフセットが行間隔の半分を超える場合、「1行分ずれた誤ペアリング」の方が正しいペアリングよりコストが低くなり、`GapCost`をどう調整しても直せないことをCodexが具体例(名前Y=[120,200],Lv Y=[88,168,248]、先頭スキルの名前欠落)で証明。自分で実データ6画像・17スキル項目を計測したところ、名前-Lv間オフセットは31px〜48pxで変動し(行間隔は約80pxでほぼ一定)、既にオフセット48pxの画像(case6、本日の別修正で使った実画像。この画像自体は名前欠落が無い(n==m)ためシフトバグそのものは起きていないが、GapCost非依存でこの構造的欠陥が起こりうる前提条件(オフセット>行間隔の半分)が実データに実在することの裏付けとなる)が実在することを確認。マッチコストを生の差ではなく「実データ平均37pxからのズレ」で評価するよう変更(`AssumedRowOffset`定数)し、Codexの反例を回帰テスト化(`PairByNearestY_LeadingNameMissing_DoesNotShiftRemainingLevels`)。**実バグ発見・修正(2)**: `TryRecoverOrphanNames`の孤児行バンド切り出し中心が、名前ではなくLv側のY座標になっていた(孤児エントリのYはLv行のY)。名前-Lv間オフセットが`halfHeight=40`を超える画像(上記48pxの画像等)では、切り出し窓の外に名前の上端がはみ出しうる欠陥。`AssumedRowOffset`を使い名前の推定位置を逆算して中心に据えるよう修正。**実バグ発見・修正(3)**: コンパニオン除外判定が`OcrLine`単位(行の左端X座標)だったため、OCRが孤児側とコンパニオン側を同一行として結合した場合、行全体のテキスト(孤児+コンパニオン混在)が正規化対象になり、`Normalize`の「長い名前優先」によりコンパニオン側の名前を誤って採用しうる欠陥があった。判定を`Word`単位(個々の単語のX座標)に変更し、孤児側の単語のみからテキストを再構成するよう修正。3件とも全176テスト成功で非破壊を確認(既存の実画像テスト15枚・37スキル項目、非護石画像30枚含む)。**軽微な指摘・対応**: `RunVariantsAndMerge`内のコメントが置き換え前の「貪欲マッチング」の説明のまま追随していなかったため修正。**過去の変更内容(再掲、参考)**: `ExtractCharmName`(59-71行目、修正前)がアンカー座標を一切使わず、画像全体で最初に見つかった「〜の護石」を護石名として返していた。左右2パネル(装備中側・BOX側等)が同時に写る画面(「装備変更」「装備の確認・売却」等)で、`FindAnchor`は正しく右パネルを選びスキルも右パネルから正しく読み取るのに、護石名だけ左パネルのものを誤って返す不整合があった。実データ`case2 equip change/20260615054234_1.jpg`(左:秘歴の護石RARE7、右:栄世の護石RARE8)で検証したところ、`Skills=[見切りLv3,属性吸収Lv1]`(右パネル、正)に対し`CharmName=秘歴の護石`(左パネル、誤)を返すことを確認。CLAUDE.mdの「種別判定(武器/防具)」ロジックは護石名ベース(「栄世の護石」なら1つ目が武器スロット)のため、この誤り単体で武器スロット判定が丸ごと外れる実害があった。`ExtractCharmName`にアンカー座標を渡し、`IsCharmPanel`と同じ相対位置範囲(dx:-50〜250,dy:80〜280)内の候補のみを対象とするよう修正(回帰テスト`ReadWithMetadataAsync_DualPanel_PicksCharmNameFromAnchoredPanel`追加、同画像でCharmName=栄世の護石を検証)。**過去データへの影響に注意**: この修正以前に読み取った`charm-lists/`のCSVで、複数パネル画面から読み取った護石は護石名・RARE値・武器スロット種別が誤っている可能性がある。再読み取りを推奨(ユーザーへ2026-07-19報告済み)。**観察点(据え置き、Codex指摘、2026-07-19時点)**: (1)~~`RunVariantsAndMerge`は名前・Lvを別バリエーションから独立選択しインデックス順にペアリングするため、片方だけ中間行が欠落すると後続が全てズレて誤ペアになりうる~~ → 2026-07-21のDPペアリング導入、および同日の`AssumedRowOffset`導入により解消済み(上記参照)。(2)「検出数最大」を品質基準にしているため誤検出込みのバリエーションが優先されうる、かつ3スキル上限のチェックが無い。(3)`FindAnchor`の3段フォールバックは「完全一致→部分一致→逆算」の順で全段を評価するため、左パネルで完全一致・右パネルで部分一致しかできない場合に左が選ばれ「常に右優先」という意図と矛盾しうる。3点とも次回大幅改修時の要再検討事項として記録

**追記(2026-07-24〜2026-07-27時点、CLAUDE.md記載の後続変更、本台帳側の精査履歴は未反映)**: ウルトラワイド(21:9)対応(LetterboxNormalizer連携・護石名先頭ゴミ文字除去)、真の21:9モニタ対応(フルスクリーンOCRの縮小コピー・パネル領域の右上コーナー基準化・アンカー未検出時の右端フォールバック)を追加。詳細はCLAUDE.mdの該当節を参照。次回本ファイルを精査する際にこれらの変更分を対象に含めること。

**2026-08-05の再精査(Codex相談)**: 上記未反映分をSlotIconAnalyzer.cs/MainWindow.xaml.csと合わせてCodexへ相談(詳細な進め方は上記SlotIconAnalyzer.csの2026-08-05追記を参照)。本ファイルに関わる指摘は以下2件。**実バグ発見・修正(重要度中、Codex指摘後にadvisorとの相談で1回設計修正)**: `FindSkillLabelAnchorCandidates`(第3フォールバック、「装備詳細」「備詳細」の2段階が両方失敗した場合のみ使われる「装備スキル」からの逆算アンカー)が、基準解像度前提の固定オフセット`-20`,`-310`を、真の21:9ネイティブでOCR用に縮小済み(`ocrScale`、例えば3440幅なら約0.744)の座標系にそのまま適用していた。自分でも計算式を検証し、逆算後の元画像座標がY方向で約100px級ずれることを確認(Codexの概算と一致)。**初版の修正(不完全、後で訂正)**: `FindAnchor`等に`ocrScale`のみを渡す修正をしたが、advisorとの相談で「黒帯付き21:9はimg.Width<=2560のため`ocrScale=1.0`のまま変化せず、Codexが指摘したもう一方のケース(黒帯付き21:9で`310×0.762`とすべきところ310のまま約74pxずれる)が未修正」との指摘を受けた。**最終版の修正**: `LetterboxNormalizer.DetectContentBounds`が返す`scale`(黒帯付き21:9でUI自体がコンテンツ高さ/1440に縮小配置される幾何比率)と`ocrScale`(真の21:9ネイティブのOCR専用縮小率)は別の量だが、両者は排他的に働く(黒帯付き21:9はocrScale=1.0、真の21:9ネイティブはscale=1.0)ため、積`scale*ocrScale`を単一の補正係数として渡すよう修正。通常16:9では両方1.0のため従来と完全に同じ挙動。**回帰テスト未追加、テストカバレッジ不明な点を明記**: 既存のテストコードに「装備スキル」という文字列への直接言及が無く、現在保有する実画像資産のいずれがこの第3フォールバック経路を実際に通っているかは未確認。数式上は正しいと検証済みだが、実データでの動作確認ができていない状態のまま修正した。**Codexが確認した他の観察点(据え置き)**: `FindAnchorWithFallbackAsync`は`FindAnchor`が非nullを返した時点(=「何らかのアンカーが見つかった」)でフォールバック探索を打ち切り、`IsCharmPanel`による「有効な護石パネルか」の確認はその後に行われる。理論上、フルOCRで非護石パネルのアンカーが先に見つかった場合、本来成功しうる狭いフォールバック幅でのOCRを試さないまま`IsCharmPanel=false`で処理終了しうる。現在確認されている画面パターン(装備変更・狩猟後護石入手・マカ錬金鑑定結果)はいずれも同一画面に護石パネルと武器/防具パネルが混在するケースが無いため未発現だが、次回大幅改修時の要再検討事項として記録。

### ImageVariantFactory.cs

状態: 済(2026-07-19、Codex相談、0件・観察点1件)

SkillReadingPipeline(5バリエーション生成・幅ガード)。LvXThreshold値(raw:300,trim60:240,otsu:300,trim60_otsu:240,gray:300)がCLAUDE.md記載と一致することを確認。`gray`変数を(3)Otsu二値化と(5)グレースケールのみバリエーションで共有する設計はCodexとの相談で問題なしと確認(Threshold/CvtColorは別Matへの書き込みのみで`gray`自体は変更されない)。`crop.Cols>60`の幅ガードで3バリエーションのみになるケースも、RunVariantsAndMerge側がバリエーション数を固定仮定していないため問題なし(残るLvXThreshold=300バリエーションではLv検出0件になり名前のみ扱いとなるが、誤データを生成するより安全な失敗)。**観察点(据え置き)**: `Create`メソッド(15-58行目)は複数のMatを順次生成し`List<Variant>`に積むが、途中でOpenCV処理が例外を投げた場合、生成済み・リスト追加前のMatがDisposeされずネイティブメモリリークする設計。呼び出し元の`try/finally`は`Create`が正常にリストを返した後にしか機能しないため防御にならない。実画像でOpenCV例外が起きる頻度は低く、呼び出し元(MainWindow)は画像単位でtry/catchして処理継続する設計のため実害は限定的だが、異常画像が多数混じった場合はネイティブメモリ累積のリスクあり。修正には`Create`全体をtry/catchで囲み例外時に確実にDisposeする必要がありコード複雑性が増すため、今回は見送り次回要再検討

### LvParser.cs

状態: 済(2026-07-19、Codex相談、実バグ1件修正)

SkillReadingPipeline(184行目、`t.X0 >= v.LvXThreshold`で右側候補のみに適用・null返却時はlvsに追加せずスキップ)。CLAUDE.md記載の文字置換仕様(し→L, l→L, I→1, ー除去)と実装が一致。**実バグ発見・修正**: 前回(2026-07-12)の観察点(OverflowExceptionリスク)を再評価したところ、「画像単位で隔離され実害なし」ではなく「その画像の読み取り結果を丸ごと失う可用性問題」と再評価。さらにCodexとの相談で、`rest.Where(char.IsDigit)`(旧20行目)が文字列中の数字を位置に関係なく全て拾って連結するため、`LV1個2`→`12`・`LV1/2`→`12`のようにOCRが余計な文字を巻き込んだ場合に例外にならず**もっともらしい誤ったLv値をサイレントに生成する**より深刻な問題を発見(オーバーフローと異なり検出不能な誤データ)。加えて`I→1`変換が残り文字列全体に適用されるため`LVSKILL2`のような別ラベル混入時に存在しない数字を生成しうる点、`char.IsDigit`が全角数字を受理するが`int.Parse`は受理せずFormatExceptionになる不一致も判明。数字抽出を`rest.TakeWhile(char.IsAsciiDigit)`(先頭からの連続数字のみ)に変更し、`int.Parse`を`int.TryParse`に変更することで、離れた数字の連結・全角数字混入・オーバーフローの3点をまとめて解消(回帰テスト`LVSKILL2`/`LV2147483648`/`LV１`→null、`LV1個2`/`LV1/2`→1を追加)。値域検証(Lv0や非現実的大値の無効化)はスキルによりLv上限が異なりパーサー側で決め打ちするリスクがあるため見送り。`.Replace("l","L")`は直後の`ToUpperInvariant()`で同じ変換が行われるため実質冗長だが、動作に影響なし(修正不要と判断)

### SkillNameNormalizer.cs

状態: 済(2026-07-21、Codex相談、実バグ1件修正)

SkillNameLoader(正解候補121種・HashSet化)・SkillReadingPipeline。**2026-07-21の再精査(Codex相談)**: カナ誤認フォールバック(2026-07-21新規追加、「力」がカタカナ「カ」に誤認識されるOCRの癖への対応)を精査。**実バグ発見・修正**: フォールバック別に全スキルを1周ずつ試す設計(素の部分一致→カナ誤認→濁点→両方)だと、「防御」⊂「防御力ＤＯＷＮ耐性」のような包含関係を持つ正規名の組で、OCRが「力」を「カ」に誤読した場合(例:「防御カＤＯＷＮ耐性」)、素の部分一致の周で短い「防御」が先に確定してしまい、カナ誤認フォールバックを要する長い「防御力ＤＯＷＮ耐性」の方が優先されるべき場面でも短い名前が採用される欠陥をCodexが発見(`skill-decoration-map.json`の実データで確認済み、6件ある包含関係の1つが該当)。4つのフォールバックをスキルごと(長い順)にまとめて試す設計に変更し、長さ優先順位を保ったまま全フォールバックを判定できるよう修正。回帰テスト2件(`KanaFallback_HijibaKa_MatchesHijibaChikara`・`KanaFallback_PrefersLongerNameOverContainedShorterName`)追加。**その他の確認結果**: 「正規名にカを含むスキルが無いため一方向の置換のみで安全」というコメントはやや強すぎる主張(OCR誤読テキストが偶然別の正規名と部分一致する可能性までは排除できない、Codex指摘)だったため、コメントを正確な主張に修正。濁点フォールバックとカナ誤認フォールバックの順序(カ→力の後に濁点除去)は現行120種のデータでは問題なし(濁点除去対象の仮名と「力」を両方含む名前が無いため複合フォールバックの実データ対象自体が無い)。全171テスト成功。**過去の変更内容(再掲、参考)**: (1)濁点フォールバック用`DakutenMap`がカタカナのガ・ザ・ダ・バ・パ行のみでひらがな未対応だった。実在する正規名「飛び込み」に対しOCRが「飛ひ込み」と誤読した場合に救済できない漏れがあったため、ひらがな版を追加(回帰テスト`DakutenFallback_Hiragana`追加)。(2)全角・半角英数字の揺れを吸収していなかった。実データに「ＫＯ術」「回避距離ＵＰ」「体力回復量ＵＰ」「防御力ＤＯＷＮ耐性」等の全角英数字混じりスキル名があり、OCRが半角(`KO術`等)で読み取ると一致しない漏れがあったため、比較前にOCRテキストの半角ASCII可視文字(!〜~)を全角化する`ToFullWidthAscii`を追加(回帰テスト`HalfWidthAlphabet_NormalizedToFullWidth`追加)。**設計強化**: Codexとの相談で、`Normalize`が`knownSkills`の「長さ降順ソート済み」という契約に完全依存しており(呼び出し元`SkillNameLoader.Load`がソート済みリストを渡すことが前提)、この契約がテストで直接検証されていない点を指摘された。実データに包含関係が6件存在(`攻撃`⊂`火属性攻撃強化`等5件、`防御`⊂`防御力ＤＯＷＮ耐性`)することを確認、現在の唯一の呼び出し元は正しくソートしているため実害はなかったが、将来別の呼び出し元が未ソートリストを渡した場合にサイレントに短い誤一致を返すリスクがあったため、`Normalize`内部で明示的に長さ降順ソートし直すよう変更し契約をメソッド自体で保証する設計に強化(回帰テスト`PartialMatch_UnsortedInput_LongerNameStillPreferred`追加、未ソート入力での長い名前優先を直接固定)。特殊・分解形式の濁点(ヴ等)は現在の121種に該当なしのため見送り(観察点)

### SkillNameLoader.cs

状態: 済(2026-07-19、Codex相談、設計強化1件)

SkillNameNormalizer(正解候補121種の供給元)・MainWindow(`LoadFromEmbeddedResource`でOCRパイプラインへ供給)。前回(2026-07-12)の実バグ発見・修正: 相互作用チェックで`skill-decoration-map.json`(正規名121種)と`RarityInference`が使う`skill-groups.json`(113種)を突き合わせたところ3件で表記が食い違っていた(防御力ＤＯＷＮ耐性/貫通弾・竜の矢強化/災禍転福 vs 半角DOWN/一矢/転覆)。`skill-name-checklist.md`/`deco_checklist.txt`で正規側が正しいと裏取りの上、`resources/skill-groups.json`の3件を正規名に修正済み。今回のCodex相談で実データ(`decorations`361件・スキル534件出現・`extra_skills`4件)を検査したが空文字/null/重複/前後空白は全て0件で現状データは正常と確認。**設計強化**: `ParseJson`(旧29-53行目)がJSONキーの空文字列・空白のみ・null(`extra_skills`側)を無検証で受理する設計だった。空のスキル名が登録されると下流の部分一致(`SkillNameNormalizer`)で`text.Contains("")`が常に真になり任意のOCR文字列を誤って空の正規名に変換しうる、`extra_skills`の`null`要素は`GetString()!`の`!`がコンパイラ警告を抑えるだけで実行時はNullReferenceExceptionになる、`extra_skills`プロパティ自体の欠落も無検証で117種のみ返してしまう、という3点のfail-fast不足をCodexに指摘され、`AddSkillName`ヘルパーで空文字/空白/null検出時に`InvalidOperationException`を投げるよう変更、`extra_skills`欠落時も例外化(回帰テスト3件追加: `Load_EmptyDecorationSkillName_Throws`/`Load_NullExtraSkill_Throws`/`Load_MissingExtraSkillsProperty_Throws`)。現状データは正常なので実害はないが、将来の手動編集ミス(今回の3件表記不一致のような)を早期検出する防御。**観察点(要調査・据え置き、前回から継続)**: decoration-mapにあってskill-groups.jsonに無いスキルが8件(飛び込み・緩衝・ジャンプ鉄人・昆虫標本の達人・クライマー・オトモへの采配・閃光強化・ハンター生活)。護石に実際つくスキルかどうかユーザー未確認のため、RarityInference.cs精査(次回以降)で扱う。

**追記(2026-07-20、宿題解消)**: 上記8スキル問題はユーザーが実機の「条件ソート→装備スキル」一覧との突き合わせで解決。7種は装飾品専用スキルと確認(対応不要)、残る1種(オトモへの采配)は防具自体にのみ付くスキルと判明し`extra_skills`から削除(正規名121種→120種)。詳細は本ログ末尾の「解消済み事項」節、またはCLAUDE.mdの「RARE推定ロジック」節を参照。

### SkillOcrTypes.cs

状態: 済(2026-07-19、Codex相談、0件)

SkillReadingPipeline(OcrTextItem/SkillEntry/SkillReadResultの生成元)・MainWindow(438-441行目、`Name`/`Lv`どちらかnullなSkillEntryは`CharmSkill`変換前にフィルタして除外する設計を確認)。データ型定義のみでロジックなし。Codexとの相談でstruct/class選択・nullable設計を再確認、`OcrTextItem`/`SkillEntry`をreadonly record structにしている点(小型データの大量生成に適切)・`SkillEntry`のnullable設計(名前/Lv部分欠落表現)は妥当と確認。**観察点(据え置き、潜在的)**: `SkillReadResult`(record、`IReadOnlyList<SkillEntry>`を保持)の自動生成Equalsはリスト内容ではなく参照を比較するため、同じ要素を持つ別インスタンスは等価にならない。現状は戻り値DTOとしてのみ使われ等価比較・HashSet・Dictionaryキー用途が無いため実害なし。将来値オブジェクトとして比較する用途が生じた場合は`ImmutableArray<SkillEntry>`化等が必要

### TextOcrReader.cs

状態: 済(2026-07-19、Codex相談、実バグ1件修正)

SkillReadingPipeline(RecognizeAsync/RecognizeBytesAsyncの両方を消費、フル画像OCR+バリエーション毎のMat直接OCR)。CLAUDE.md記載の仕様(BitmapDecoder.GetSoftwareBitmapAsyncでの直接デコード、Convert方式回避によるJPEGアルファ破損対策)と実装が一致。RecognizeBytesAsyncはOpenCVのBGR→BGRA変換後のバイト列を直接コピーする方式でJPEGデコードを経由しないため同アルファ問題の対象外。Dispose周り(BitmapDecoder/OcrEngineがIDisposable非実装、SoftwareBitmap/ファイルストリームはusing済み)はCodexとの相談でリーク無しと確認。**実バグ発見・修正**: `RecognizeBytesAsync`の`bitmap.CopyFromBuffer(bgraBytes.AsBuffer())`が、渡すバイト数が`width*height*4`より短い場合は例外になる一方、**長い場合は無検証で受理**され「先頭部分を別サイズの画像として処理し無音で誤認識する」可能性があるとCodexが実測で確認(HRESULT 0xC00D7170等の詳細も確認済み)。現在の唯一の呼び出し元(`SkillReadingPipeline.OcrMatAsync`)は`bgra.Rows*bgra.Cols*4`で正確に計算しており実害は無いが、これまでの精査(LvParser・SkillNameNormalizer等)と同様「呼び出し元の契約への暗黙依存」パターンのため、事前に`width*height*4`とバイト数の完全一致を検証し不一致時に`ArgumentException`を投げるガードを追加(回帰テスト`RecognizeBytesAsync_MismatchedLength_Throws`追加)。観察点(据え置き、前回から継続): `CreateEngine`が呼び出しごとに`OcrEngine.TryCreateFromLanguage`を再実行しキャッシュしない(1画像あたり最大6回)。導入するなら言語タグ別`ConcurrentDictionary<string,Lazy<OcrEngine>>`が必要だが、現状は逐次処理のため性能上の緊急度は低いとCodexも同意、今回も見送り

### DuplicateChecker.cs

状態: 済(2026-07-19、Codex相談、0件・観察点3件)

CharmModel(スキル・スロット比較)・DuplicateCheckWindow(消費者)。CLAUDE.mdの完全同一/完全上位互換/比較不能の仕様と`IsStrictlySuperior`/`CompareSkillLevels`/`CompareSlots`の実装が一致することを確認。`FindInferiors`のO(n^2)ループが(i,j)と(j,i)を両方評価する非効率はCodexとの相談で正しさに影響しないことを確認(`IsStrictlySuperior`が方向付きのため両方向の呼び出しが必要、性能改善の余地はあるが優先度低)。**観察点(据え置き、3件とも「現在の全生成経路が3要素を保証する契約を守る限り到達しない」(b)級と判断)**: (1)`IdentityKey`(83行目、文字列join)は末尾の0を省略した`[3,1]`と`[3,1,0]`を異なるキーとして扱うが、`SlotsGte`(145行目)は不足位置を0埋めして比較するため上位比較では同一と判断される不整合がある。`Charm.ArmorSlots`/`WeaponSlots`(`CharmModel.cs`21-22行目)は型レベルでは3要素を強制していない(`List<int>{get;init;}=[]`)が、CLAUDE.mdのCSVフォーマット(12列固定)・`SlotValidation`(常に3要素返却)により現在の全生成経路は3要素を保証していると判断。(2)`IdentityKey`の文字列キー生成(`|`,`,`,`#`,`:`区切り)は理論上スキル名に区切り文字が含まれるとキー衝突しうるが、実データ121種を検査し該当スキル名0件と確認済み(正規化済み121種のみ受理する運用のため)。(3)重複スキル名構成(`[攻撃Lv4,攻撃Lv2]`等)は上位比較では`Incomparable`扱いだが完全一致検出(`IdentityKey`)には同様のチェックがなく、双方が同一の不正構成なら重複グループになる不整合がある。重複スキル名自体が正常な生成経路(OCR/CSV/手動入力)では発生しない異常データのため実害なしと判断

### RarityInference.cs

状態: 済(2026-07-19)+変更(2026-07-21、Codex相談で共有ロジック部分は再精査済み、下記参照)

skill-groups.json/charm-combinations.json・CharmEditWindow・MainWindow(CSV一括推定)・CharmProbabilityEstimator(新規)・CharmTheoreticalValueChecker(新規)。**2026-07-21の変更**: `Infer`内の共通処理を`ResolvePossibleGroups`/`MatchesGroupPattern`として抽出しinternal化、新規2ファイルと共有。`CharmCombinationSlot`に`Weight`フィールド追加(RARE8スロット確率用)。さらに`CharmProbabilityEstimator`側にあった`FindMatchingSlot`(スロットパターン完全一致判定)も本日`internal static`として本ファイルへ集約し、`CharmTheoreticalValueChecker`からも共有できるようにした(追加的な抽出のみで`Infer`自体の挙動は変更なし)。共有ロジック部分は本日のCodex相談(下記CharmProbabilityEstimator.cs/CharmTheoreticalValueChecker.cs参照)で「同一レアリティ内での複数パターン同時マッチは実データ上0件」「ただしスキル入力順序によって`MatchesGroupPattern`が不一致になり得る」ことを確認済み。`Infer`本体(RARE7/8特殊処理等)自体はこの変更の影響を受けていないため2026-07-19時点の精査結果を維持。CLAUDE.md記載の優先順位(武器スロット→RARE8固定、研鑽→RARE5固定、グループマッチング)と実装が一致。RARE7/8特殊処理(66-71行目、両方にマッチしたら8を除外)はCodexとの相談で実データ検証(RARE8の全8パターンがRARE7と同一SkillGroups+全スロット候補に武器スロットあり)により現在のテーブル構造では正しく必要な処理と確認。**設計強化**: `skill-groups.json`(269エントリ)の`(Name,Level)`重複検出が無く、`FirstOrDefault`(47行目)が最初の1件を無条件採用する設計だった。現状重複0件で実害はないが、`SkillNameLoader`(#8)と同じ精神(手編集データは実質境界、過去に実際表記ミスが3件発生した実績あり)でfail-fastガードを追加。`ParseSkillGroups`を`internal`化しテストから直接検証できるよう`CharmChecker.Core/AssemblyInfo.cs`を新規作成し`InternalsVisibleTo("CharmChecker.Tests")`を追加(このファイルは`CharmChecker.App/AssemblyInfo.cs`とは別物、低優先度のAssemblyInfo.cs精査(#23)で扱う際に混同注意)。回帰テスト`ParseSkillGroups_DuplicateNameLevel_Throws`追加。**観察点(据え置き、(b)級)**: (1)RARE7/8特殊処理は現在のデータでは正しいが、将来「RARE7と対にならないRARE8パターン」が追加されると`matchedRarities.Count==1`で武器スロットなしでも8を返してしまう。(2)キャッシュ済みリソース(`LoadSkillGroups`/`LoadCombinations`が返す配列・`int[]`)は呼び出し元が変更すればLazyキャッシュされたグローバルデータが以後全て破損しうるが、現在の呼び出し元は読み取り専用のため実害なし。**宿題(ユーザー確認待ち、2026-07-19)**: `skill-decoration-map.json`にあって`skill-groups.json`に無い8スキル(オトモへの采配・クライマー・ジャンプ鉄人・ハンター生活・昆虫標本の達人・緩衝・閃光強化・飛び込み)が実際に護石につくスキルか未確認。該当スキルを含む護石は`Infer()`が49-50行目でサイレントにnullを返す(RARE推定失敗)。ユーザーが後日確認予定。**実バグ発見・修正(2026-07-21、ユーザー承認の上で本格対応)**: `MatchesGroupPattern`(旧84-93行目)は`charm.Skills`の並び順とcombo側`SkillGroups`配列の並び順を位置ごとに突き合わせる仕様のため、同じスキル集合でも入力順序が異なると不一致になる欠陥があった。具体例で確認: RARE5の`[1,7]`(ＫＯ術Lv1+ひるみ軽減Lv3の順)は`charm-combinations.json`に実在するが、逆順`[7,1]`のエントリは存在しない(Pythonで直接検索し不在を確認)。OCR読み取りは画面表示順=データ側の順序と一致するため2026-07-21の実機検証(#7)では顕在化しなかったが、手動入力・CSVインポートでスキルを逆順に入れると`RarityInference.Infer`・`CharmProbabilityEstimator.Estimate`・`CharmTheoreticalValueChecker.IsTheoretical`の3つ全てが本来一致するはずの組み合わせを「一致なし」(null)として扱っていた。発見当初はコアロジック(`Infer`の`matchedRarities.Count==1`判定・RARE7/8特殊処理)への波及リスクが未検証だったため一旦見送り記録としたが、ユーザーの指示で本格対応。**独立レビュー(Codex、必須ゲートB)**: 修正案を「(A)順序非依存マッチング(全順列/一対一割り当て探索)」「(B)入力側でスキル順を正規化」の2案で相談。Codexが実データ全件(charm-combinations.json 29件・skill-groups.json 269件、複数グループ所属20件全て`groups:[2,4]`)を独立検証し、(1)案Bは元データ自体が昇順ソートすらされていない(29件中14件)ため「正規の順序」を定義する根拠が無く却下、(2)案A採用後も同一レアリティ内で複数comboに同時マッチする新規曖昧性は0件(11種のグループ集合による全1〜3スキル組み合わせを網羅検証)、(3)RARE7/8特殊処理(全8パターンが配列順まで完全一致)への悪影響も無し、と結論。**修正内容**: `MatchesGroupPattern`をバックトラッキングによる一対一割り当て探索(`TryAssign`、パターン位置ごとに未使用のスキルを1つ割り当てられるか再帰探索、最大3スキルのため計算コストは無視できる)に置き換え。`Charm.Skills`自体の順序は変更しない(表示順・CSV出力順に影響なし)。**回帰テスト追加**: `RarityInferenceTests.cs`に(a)RARE5`[1,7]`逆順一致、(b)`[4,1,1]`(超会心+group1スキル2つ)で同じスキルを2位置に使い回さないことの検証、(c)複数グループ所属スキル(「会心撃【属性】」Lv2、groups:[2,4])が順序に関わらず一致する検証(Theory、2パターン)、(d)実データ全件で同一レアリティ内の複数combo同時一致が無いことを固定するデータ整合性テスト、を追加。`CharmProbabilityEstimatorTests.cs`/`CharmTheoreticalValueCheckerTests.cs`にも逆順で同じ結果になる回帰テストを追加(計7件)。全168テスト成功

**追記(2026-07-20、宿題解消)**: 上記8スキル問題は解消済み(詳細はSkillNameLoader.cs見出しの追記、およびCLAUDE.mdを参照)。

### CharmProbabilityEstimator.cs

状態: 済(2026-07-21、Codex相談、実バグなし・共有ロジック集約1件・未修正の観察点1件)

RarityInference(`ResolvePossibleGroups`/`MatchesGroupPattern`/`FindMatchingSlot`を共有)・MainWindow(詳細パネル表示)。2026-07-21新規実装機能の初回精査。Codexとの相談で「同一レアリティ内の複数パターン同時マッチ」の曖昧性リスクを検証したが、実データ(`charm-combinations.json`全エントリ)では同一レアリティ・同一スキル数で複数パターンが同時マッチする実例は0件と確認(`FirstOrDefault`が最初の1件を無条件採用する設計だが、現状は事実上一意に決まるため実害なし)。**共有ロジック集約**: 本ファイル固有だった`FindMatchingSlot`(スロット完全一致判定)を`RarityInference.FindMatchingSlot`(internal)へ抽出・移設し、`CharmTheoreticalValueChecker`からも再利用できるようにした(追加的な変更のみで本ファイルの計算結果自体は不変、既存2テストで再確認)。**追記(2026-07-21、同日中に修正)**: `MatchesGroupPattern`のスキル入力順序依存バグ(詳細は上記RarityInference.cs行を参照)は本ファイルの`Estimate`にも伝播していたが、共有ロジック側の修正(順序非依存化)により解消。回帰テスト`SkillOrderReversed_ProducesSameProbability`追加、全168テスト成功

### CharmTheoreticalValueChecker.cs

状態: 済(2026-07-21、Codex相談、実バグ1件修正+テスト強化2件)

RarityInference(`ResolvePossibleGroups`/`MatchesGroupPattern`/`FindMatchingSlot`を共有)・CharmProbabilityEstimator(スロット一致判定ロジックを共有)・MainWindow(詳細パネル表示)。2026-07-21新規実装機能の初回精査。**実バグ発見・修正**: `IsSlotPatternDominated`による「支配されなければtrue(理論値)」という判定のみで、護石自身のスロット構成が`charm-combinations.json`に実在するパターンかどうかを一切検証していなかった。実在しない極端な値(例: このスキルグループ構成のテーブル上限を超える防具Lv3)は、テーブル上のどのパターンにも数値的に上回られないため誤って理論値=trueと判定されてしまう(Codexが指摘、具体例で再現確認: ＫＯ術Lv3+ひるみ軽減Lv3・防具[3,0,0]・RARE7で修正前はtrueを返していた)。この構成はUI(ComboBoxで防具Lv3を選択可能)・CSVインポート(スロット値3を受理)の両経路から到達可能なため(a)級と判断し、`comparableSlots`との完全一致を`RarityInference.FindMatchingSlot`で先に要求し、一致が無ければ支配判定に進まずnullを返すよう修正(回帰テスト`ImpossibleSlotConfiguration_ReturnsNull`追加)。既存7テスト全てが新ロジックでも成立することを一つずつ手計算で確認済み(特に`Rare7Slot20`の防具[2,0,0]がRARE7側`[2,0]`(武器なし)との完全一致を経てRARE8側のどの構成にも支配されず理論値=trueになる経路、`SlotDominatedByAnotherPattern`の防具[0,0,0]+武器1がRARE8`[0]+武器1`と完全一致した上で支配されてfalseになる経路)。**テスト強化**: `SkillWithNoHigherLevelAnywhere_StillCountsAsMax`が実際には`IsTheoretical`を一切呼ばず`skill-groups.json`のデータ内容を確認するだけの弱いテストだったため、`SkillWithNoHigherLevelAnywhere_DoesNotIncorrectlyFailMaxCheck`に置き換え、超会心Lv1単独護石で「所属グループの最大Lv基準(誤)ならfalseになるがスキル自身の最大Lv基準(正)ならnullになる」ことを直接検証するよう強化。曖昧マッチ(同一レアリティ内複数パターン同時一致)はCodexとの相談で実データ0件と確認済み(CharmProbabilityEstimator.cs行のCodex確認と同一調査)。**追記(2026-07-21、同日中に修正)**: `MatchesGroupPattern`のスキル入力順序依存バグ(詳細は上記RarityInference.cs行を参照)は本ファイルの`IsTheoretical`にも伝播していたが、共有ロジック側の修正(順序非依存化)により解消。回帰テスト`SkillOrderReversed_StillReturnsTrue`追加、全168テスト成功

### CharmTypeLoader.cs

状態: 済(2026-07-19、Codex相談、実バグ1件修正(MainWindow側)+設計強化1件+観察点2件)

charm-types.json・SkillReadingPipeline(武器スロット有無判定)・MainWindow(ReadScreenshotでのRarity設定)・RarityInference(InferRarityBatchでの補完)。専用テストが存在しなかったため`CharmTypeLoaderTests.cs`を新規作成(8件)。**実バグ発見・修正(MainWindow.xaml.cs側、重要度高)**: 相互作用チェックで`MainWindow.xaml.cs`の`ReadScreenshot`(450行目)を確認したところ、`charm.Rarity = charmType?.Rarity`で`CharmTypeLoader.Lookup`の結果のみを設定しており、`charm-types.json`の4件(未解/史伝/秘歴/栄世)にマッチしない一般護石・「希望の護石」等は`Rarity`が`null`のままになっていた。`RarityInference.Infer`による補完(`InferRarityBatch`)は`CharmCsvConverter.ParseText`後(CSVインポート時、654-655行目)にしか呼ばれておらず、スクショ読み取り直後には一切呼ばれていなかったため、スクショから一般護石を読み取ると一覧でRarityが常に「-」表示になる実装漏れがあった(ユーザーへ確認の上、未意図と判明)。`ReadScreenshot`のループ末尾に`InferRarityBatch(results.Select(r => r.Charm).ToList())`を追加して修正(UI層のためCharmChecker.Testsの自動テスト対象外、ビルド成功のみ確認)。**設計強化**: `ParseJson`(旧40-52行目)にSkillNameLoader/RarityInferenceと同様の空文字列・null・重複名検証が無かったため、`AddSkillName`と同様のfail-fastガードを追加(`ParseJson`をinternal化、回帰テスト`ParseJson_EmptyName_Throws`/`ParseJson_DuplicateName_Throws`追加)。**観察点(据え置き、(b)級)**: `Lookup`(29-38行目)の末尾一致検索が「入力順の最初一致」であり、将来`charm-types.json`に部分文字列関係の護石名(例:「新・栄世の護石」)が追加されると誤マッチしうる。現行4件は相互に末尾包含関係が無く実害なし、次回データ追加時に最長一致への変更を再検討

**追記(2026-08-05、呼び出し契約の変更、詳細はMainWindow.xaml.csの同日追記を参照)**: `Lookup`が`null`を返すケース(護石名OCR失敗、または未登録護石名)について、呼び出し元`MainWindow.ReadScreenshot`が`charmType?.HasWeaponSlot ?? false`とフォールバックしていたため、栄世の護石(唯一`hasWeaponSlot=true`)の護石名抽出に失敗すると武器スロットが防具スロットとして誤保存される実バグがあった。`CharmTypeLoader.cs`自体の実装(`Lookup`が`null`を返す仕様そのもの)に問題はなく、呼び出し元が`null`を安全側に処理していなかったのが原因。呼び出し元を「`charmType is null`ならスロット読み取りごとスキップ」に変更して解消(`CharmTypeLoader.cs`側の変更なし)。

## 中優先（データモデル・UI本体）

### CharmModel.cs

状態: 済(2026-07-19、Codex相談、0件・観察点3件)

DuplicateChecker・CharmCsvConverter・RarityInference・MainWindow・CharmEditWindow(共通データモデル)。`Skills`/`ArmorSlots`/`WeaponSlots`は`init`のみで可変`List<T>`だが、消費側(`DuplicateChecker.SortDesc`)が`new List<int>(slots)`で防御的コピーしてからソートしておりエイリアシングによる原本破壊は無いことを確認。前回(2026-07-12)観察点: `GameVersion.Ascendance`は現状どの生成経路でも明示的に設定されず常定値`Wilds`のまま、将来拡張向け先行スキャフォールドと判断(継続)。**観察点(据え置き、3件とも(b)級、Codexとの相談で確認)**: (1)`Rarity`のみ`{get;set;}`で他は`init`という非対称性は事後推定(RarityInference)のため設計意図と一致し妥当だが、`Skills`/`ArmorSlots`/`WeaponSlots`は`init`でもリスト内容自体は変更可能なため、推定後にリスト内容を直接変更すると`Rarity`が古いまま残るリスクがある。現在のアプリは編集時に新しい`Charm`へ置き換える設計でリストを直接変更する箇所は無く実害なし。(2)`ArmorSlots`/`WeaponSlots`を3要素を型で強制する値オブジェクト(`SlotSet`等)に変更する案は、変更がCSV変換・OCRスロット検証・重複/RARE判定・メイン画面・編集画面等7ファイルに波及するため、現在の全生成経路が3要素を保証している以上は移行コストに見合わないと判断(アセンダンス対応等モデル拡張時に再検討)。(3)`CharmSource`enumの既定値0が`Screenshot`のため、将来新しい生成経路で`Source`設定を忘れるとスクショ由来として誤扱いされるenum既定値の罠があるが、現在の本番生成経路は全て明示設定済みで実害なし

### CharmCsvConverter.cs

状態: 済(2026-07-19、Codex相談、実バグ2件修正+観察点2件)

CharmModel・MainWindow(インポート/エクスポート)。正規スキル名121種にカンマ・クォート・改行を含むものは無く、単純な`line.Split(',')`によるCSVパースに実害はないことを確認。`ParseLine`が常にArmorSlots/WeaponSlotsへ`MaxSlots=3`要素を追加する設計は、#11/#14で観察点記録した「Charmの全生成経路が3要素を保証する契約」の実際の保証元であることを確認。**実バグ発見・修正(CSVは外部データ境界のため(a)級と判断)**: (1)`ParseLine`(旧47行目)が名前ありLv0・名前なしLv正のような半端な入力をエラーにせずサイレントにスキルごと破棄していた。mhwilds.wiki-db.com等外部ツール由来・手動編集CSVでこのパターンが起こりうるため、名前・Lvの整合(名前ありLv>0、または名前なしLv0の2状態以外)を検証し`FormatException`を投げるよう変更(回帰テスト`ParseLine_NameLvMismatch_ThrowsFormatException`追加)。(2)スロット値の範囲検証が無く`-1`や`999`等を無検証で格納していたため、`ParseSlotValue`ヘルパーで範囲検証するよう変更(回帰テスト`ParseLine_SlotOutOfRange_ThrowsFormatException`追加)。**追記(2026-07-19、#24 README.md執筆中に発見・修正)**: この時点での上限は誤って`4`にしていた。CLAUDE.mdのCSV仕様(「スロットは各位置のレベルを0/1/2/3で記録する」)と不一致で、現行ゲームの最大Lv(防具Lv3・武器Lv1)を超える値をCSVインポート時に無検証で受理してしまう実バグだった。Codexとの相談(README記述の裏取り中に発覚)を経て上限を`3`に修正、境界値テスト(`ParseLine_SlotOutOfRange_ThrowsFormatException`にLv4境界追加、`ParseLine_SlotValue3_IsAccepted`新設)を追加。**観察点(据え置き)**: (1)未知のスキル名(正規121種以外)も正のLvがあれば有効スキルとして受理してしまう。将来のゲームアップデートでスキル追加された際、`resources/`データ更新前でもCSV取込自体は可能にしておきたいというトレードオフがあるため、ユーザー判断で見送り。(2)`ParseText`(72行目)が`RemoveEmptyEntries`後の添字を行番号として使うため、CSV内に空行があるとエラー時の報告行番号が実ファイルの物理行番号とずれる(低優先度、Codex指摘のみでユーザー確認は今回省略)

### MainWindow.xaml.cs

状態: 済(2026-07-21、Codex相談、軽微な不具合1件修正)

全モジュールのオーケストレーション(SkillReadingPipeline・SlotIconAnalyzer・CharmCsvConverter・DuplicateChecker・RarityInference・CharmTypeLoader・ErrorLogger・GameSkillOrder・SettingsWindow・CharmEditWindow・DuplicateCheckWindow全てと接続する中枢)。前回(2026-07-12)`_screenshotFolder`同期漏れ修正済み。AboutMenuの表示ライブラリバージョンはcsprojと一致確認済み。**実バグ発見・修正(重要度高、RARE未推定の連鎖)**: `ImportLocalDataMenu_Click`(ローカルJSONインポート)に`InferRarityBatch`呼び出しが無く、外部JSON由来のRarity未設定護石がそのまま取り込まれていた。さらに`LoadCharms`(アプリ起動時)にも同様の補完が無く、#13で修正した「スクショ読み取り時Rarity未推定」バグより前に保存された既存の`charms.json`データはアプリ再起動しても永久にRarity不明のままだった。両メソッドに`InferRarityBatch`呼び出しを追加(前者はJSONパース直後、後者はAddCharm前)。**実バグ発見・修正(ウォーターマーク方式、前回2026-07-12はユーザー判断で見送りだったが、今回ExtractCharmName誤帰属バグの実例を踏まえ再評価・対応)**: 差分読み込みが「既存護石の最新SourceTimestampより新しいファイルのみ対象」というウォーターマーク方式のため、誤った結果を「成功」として保存した画像(今回発見した護石名誤帰属等)や読み取り失敗した画像が、後続ファイルの成功によって永久に再スキャン対象外になるリスクがあった。(1)XAML側に「フォルダ内の全画像を再スキャン」チェックボックス(`RescanAllCheckBox`)を追加し、チェック時はウォーターマークを無視して全画像を対象にできるようにした。(2)`validSkills.Count==0`(護石パネルは検出できたがスキル1件も読めなかったケース)を`failed`カウント・`ErrorLogger`記録の対象に変更(前回は無言`continue`でユーザーが原因を区別できなかった)。**実バグ発見・修正(UI状態異常系)**: (1)`StartReading_Click`全体を守る`try/finally`が無く、途中で例外が起きると開始ボタンが無効のまま残りうる設計だったため、メイン処理をtryで囲み`StartReadingButton.IsEnabled=true`をfinallyに移動。(2)`SaveSettings`に例外処理が無く、書き込み不可環境等で例外が起きると`MainWindow_Closing`内の後続`SaveCharms`が実行されずデータ損失につながる設計だったため、`SaveCharms`と同様のtry-catch+ErrorLogger記録を追加。実機でアプリを起動しスクリーンショットで新規UIの表示を確認済み(チェックボックス配置・フォント継承に問題なし)。**観察点(据え置き、(b)級)**: (1)`IsExactDuplicate`(710-734行目)が`DuplicateChecker.IdentityKey`とほぼ同じロジックを独自に再実装しているコード重複。Codexとの相談で現時点の判定結果は完全に同一(正規スキル名・同じ長さのスロットリストでは差異なし)と確認、共通の`CharmIdentityComparer`等への抽出は次回大幅改修時の要検討事項。(2)武器スロット表示が`CharmListItem.FormatSlotsStatic`(全件表示)と詳細パネル(`weaponLv[0]`のみ表示)で異なる。現行ゲームでは武器スロット常に0か1個のため実害なし、CSV/JSON不正入力時のみ表示差が生じうる。(3)`ReadSlots`/`DetectInRegion`(513-557行目)はBOX側`gray`生成後にDetail側処理で例外が起きると`boxGray`がDisposeされずに残る経路がある。画像処理を継続するバッチ処理のため理論上はネイティブメモリ蓄積リスクがあるが、発生頻度は低いと判断し見送り。**2026-07-21の変更内容**: `ReadSlots`のBOX/Detail同数タイの優先順位を`>`→`>=`に変更(Detail領域が誤検出しうる画面パターンへの対応)。護石一覧詳細パネルに構成確率・理論値判定の表示を追加(`CharmProbabilityEstimator`/`CharmTheoreticalValueChecker`呼び出し)。**2026-07-21の再精査(Codex相談)**: 上記2変更を精査。`ReadSlots`の`>=`変更自体・両フレーム0件のフォールバック・呼び出し先2メソッドの例外安全性(いずれも例外を投げない設計と確認済み)には問題なし。**軽微な不具合発見・修正**: `FormatProbability`(有効数字3桁の%表示)が、丸めで桁上がりする場合(例: 9.996%→単純に小数点2桁で丸めると10.00%になり有効数字4桁になる)に小数桁数を再計算しておらず、表示は破綻しないものの仕様(有効数字3桁)から外れる値を返しうるとCodexが指摘。桁上がり後の桁数で小数桁数を再計算するよう修正。`FormatProbability`を`internal`化し新規`MainWindowTests.cs`で回帰テスト4件(null・0・桁上がり2パターン・通常値)を追加。全176テスト成功

**追記(2026-07-27時点、CLAUDE.md記載の後続変更、本台帳側の精査履歴は未反映)**: `ReadSlots`の採用条件を「box>=detかつbox>0ならbox優先」から「box>0なら常にbox優先」に変更(マカ錬金鑑定結果画面での実バグ発見・修正、回帰テスト`SlotIconPipelineTests.ReadSlots_BoxUndercountsDetail_StillPrefersBoxOverSpuriousDetailRegion`追加)。`DetectInRegion`/`ClassifyFrames`をカラーMat伝播に変更(空スロットテンプレート照合のため)。詳細はCLAUDE.mdの該当節を参照。次回本ファイルを精査する際にこれらの変更分を対象に含めること。

**2026-08-05の再精査(Codex相談)**: 上記未反映分をSlotIconAnalyzer.cs/SkillReadingPipeline.csと合わせてCodexへ相談(詳細な進め方は上記SlotIconAnalyzer.csの2026-08-05追記を参照)。本ファイルに関わる指摘は以下2件、いずれも重要度高。**実バグ発見・修正(1)**: `ReadSlots`(594行目)はBOX・Detail両領域とも検出フレーム0件の場合、例外を投げず`ClassifyFrames`が空リストのままarmorSlots=[0,0,0]・weaponSlots=[0,0,0]を返し、これが「正常なスロットなし護石」としてCSVへサイレントに保存されていた。`charm-combinations.json`の全29パターン(RARE5〜8)を検証しスロット完全ゼロの組み合わせが存在しないことを裏取りした上で(CLAUDE.mdの「穴なしスロットは対象外」という記述の一次データでの確認)、0件を検出失敗として扱うことを確定。新規`SlotDetectionFailedException`を追加し、`ReadSlots`の`frames.Count==0`時にスローするよう修正。呼び出し元(`ReadScreenshot`)に専用catch節を追加し`failed`カウントへ計上(回帰テスト`SlotIconPipelineTests.ReadSlots_NoFramesDetectedInEitherRegion_ThrowsSlotDetectionFailedException`追加、単色ダミー画像で両領域0件を再現)。**波及影響(advisor指摘、記録)**: この除外パスも、下記(2)の護石名不明除外と同様に差分読み込みのウォーターマーク方式(最新`SourceTimestamp`より新しいファイルのみ対象)と組み合わさると、タイムスタンプ順で後続に成功画像がある場合に恒久的に再スキャン対象外になる。`RescanAllCheckBox`で緩和可能。**実バグ発見・修正(2、新規発見、Codex指摘後にadvisorとの相談で1回設計修正)**: `ReadScreenshot`(507-508行目)は`CharmTypeLoader.Lookup`が`null`(護石名OCR失敗、または`charm-types.json`未登録の護石名)を返した場合、`charmType?.HasWeaponSlot ?? false`により武器スロット有無を無条件で`false`扱いにしていた。`charm-types.json`は現状4件のみで武器スロットを持つのは栄世の護石(RARE8)だけのため、栄世の護石の護石名抽出にだけ失敗した場合、`ClassifyFrames`内で武器スロットとして扱うべき1つ目のフレームが防具スロットとして誤保存される。スロット値自体は妥当な範囲に収まるため`SlotValidation`では検出できないサイレント誤りだった。**初版の修正(過剰、後で訂正)**: `charmType is null`なら常にスロット読み取りごと除外する実装にしたが、内蔵advisorとの相談で「`charm-types.json`の4件にマッチしない一般護石・希望の護石等は元々`Rarity=null`のまま正常に扱われる設計(2026-07-19の`CharmTypeLoader.cs`精査参照)」との指摘を受けた。実際に`assets/`全78枚を`ReadWithMetadataAsync`+`Lookup`でプローブしたところ、`CharmName`が非nullの77枚は全て4件のいずれかに解決し(unresolvedCount=0)、この資産だけでは過剰除外の実例は確認できなかったが、資産に「希望の護石」等の未登録護石画像が単に含まれていないだけの可能性が高いと判断。**最終版の修正**: 除外条件を`charmType is null`から`readResult.CharmName is null`(護石名テキスト自体が読み取れない場合のみ)に変更。護石名は読めたが`charm-types.json`に未登録(希望の護石等)の場合は、未登録=栄世の護石ではないと確定できるため誤混同のリスクが無く、従来通り`hasWeaponSlot=false`・`Rarity=null`(後続`InferRarityBatch`で補完)のまま処理を続ける。UIイベントハンドラ内のロジックのため専用の自動テストは追加せず、ビルド成功+既存221テスト非破壊のみ確認(2026-07-19の`InferRarityBatch`追加時と同じ扱い)。**波及影響(advisor指摘、記録)**: この新しい除外パス(護石名完全に不明)は、差分読み込みのウォーターマーク方式(最新`SourceTimestamp`より新しいファイルのみ対象)と組み合わさると、除外された画像がタイムスタンプ順で後続の成功画像より前にあった場合、恒久的に再スキャン対象外になる。修正前は「誤ったデータが見える形で保存される」問題だったが、修正後は「何も保存されず静かに消える」形に変わる。`RescanAllCheckBox`(2026-07-21追加)で緩和可能。**判明したドキュメント陳腐化(未対応、次回要修正)**: `DecorationEquippedException.cs`のXMLコメントが「列プロファイル方式(`ClassifyLevel`)」という2026-07-27に削除済みの旧方式を参照したまま。今回の精査範囲外のため未修正、次回精査で対応。

### CharmEditWindow.xaml.cs

状態: 済(2026-07-19、Codex相談、実バグ2件修正、実機動作確認済み)

SkillNameLoader・GameSkillOrder・RarityInference(レアリティ動的更新)。`BuildCharm`が常にArmorSlots/WeaponSlotsを3要素固定リストで生成する設計は「Charmの全生成経路が3要素を保証する契約」の保証元の1つと確認。**実バグ発見・修正(実データ検証済み)**: スキルLvのComboBox選択肢が全スキル共通で0〜4固定だが、実データを検査したところ`skill-groups.json`の113種中**109種**(96%)は実際のLv上限が2または3で、Lv4を超えるスキルは0件だった。ユーザーが実在しない(スキル名,Lv)組み合わせ(例:「ＫＯ術Lv4」、実際の上限はLv3)を選んで保存すると、`RarityInference.Infer`が該当エントリを見つけられずRARE推定が常に「-」になる実害があった。`OkButton_Click`にskill-groups.jsonとの照合検証を追加し、候補内スキルで存在しない(Name,Lv)の場合は保存前にエラーダイアログで拒否するよう修正(実機で「ＫＯ術Lv4」を入力→OK押下→「『ＫＯ術』にLv4は存在しません。」のエラーダイアログを確認)。**実バグ発見・修正**: 同じスキル名を複数のスキル欄に入力しても拒否されず、`DuplicateChecker`が重複スキル名を比較不能として扱う不正データを手動入力から生成できたため、`OkButton_Click`に重複スキル名検証を追加(保存前にエラーダイアログで拒否)。**観察点(据え置き)**: (1)`_skillNames`(コンストラクタ、`GameSkillOrder.Order`と`skill-groups.json`の積集合)により、#12で発見した8スキル(decoration-mapにあってskill-groups.jsonに無い)がComboBoxの候補からも一切除外される。もし8スキルが実際に護石に付くなら、候補選択・RARE推定の両方に影響する共通データ不備。ユーザー確認待ちの宿題(#12)と同根のため、そちらの回答待ち。(2)`BuildCharm`(122行目)の`name!=""&&lv>0`によるサイレントスキップは、入力途中のリアルタイムプレビューでは妥当だがOK押下時(保存確定)も同じロジックのため、名前ありLv0等の半端な入力が無言で消える。CSVとは異なりUIの入力欄は「埋まっているか空か」の2値的操作なので実害小と判断し見送り。(3)手動入力時もwpfui TitleBarの表示が「護石の編集」のまま変わらない(実機で確認、コンストラクタの`Title=`代入はTitleBar.Titleに反映されない)。表示上の軽微な不整合、次回UI改修時に要対応

**追記(2026-07-20、宿題解消)**: 上記(1)の8スキル問題は解消済み(詳細はSkillNameLoader.cs見出しの追記、およびCLAUDE.mdを参照)。

### DuplicateCheckWindow.xaml.cs

状態: 済(2026-07-19、Codex相談、実バグ1件修正+観察点1件)

DuplicateChecker(消費者、`Check()`が返す`Indices`/`TargetIndex`/`SuperiorIndices`は`charms`リストの0始まり位置)・MainWindow(`CharmItems`をそのまま渡す、DataGridの列ソートは表示のみでコレクション自体の順序を変えないため`charmItems[i]`との対応がずれないことを確認)。`ShowDialog()`によるモーダル表示のため計算後の並行変更リスクも無し。#11で記録した`DuplicateChecker`側の観察点3件(IdentityKeyの末尾0省略・区切り文字衝突・重複スキル名構成)は現在の正規名・3要素スロット契約下では本ウィンドウの表示にも影響しないことをCodexとの相談で確認。**実バグ発見・修正**: `Save_Click`(102-115行目)の`File.WriteAllText`にI/O例外処理が無く、他のエクスポート処理(`MainWindow.ExportFile_Click`等)と一貫性が無かったため、try-catch+`ErrorLogger.Log`+エラーダイアログを追加(`Wpf.Ui.Controls`の`MessageBox`と`System.Windows.MessageBox`の名前衝突があったため完全修飾名で解決)。**観察点(据え置き)**: 完全同一グループの見出し(58行目)はグループ先頭要素のRarityのみを表示するが、`DuplicateChecker`はRarityを同一判定条件に含めないため、理論上は同じ構成でRarity=7とRarity=nullが混在するグループがあり得る(CharmTypeLoaderベースの直接マッピングとRarityInferenceの推定が食い違うケース等)。今回のセッションで修正した「RARE未推定の連鎖」(#13/#16)により実害は大幅に軽減されているため見送り

### SettingsWindow.xaml.cs

状態: 済(2026-07-19、Codex相談、0件)

MainWindow(設定永続化: ウィンドウサイズ・位置・スクショフォルダ)。前回(2026-07-12)の実バグ(`_screenshotFolder`影フィールドの同期漏れ)はMainWindow側で修正済み。今回のCodex相談で`OkButton_Click`の未検証入力(`FolderPathBox.Text`をそのまま`ScreenshotFolder`にセット)を再検証したが、`FolderPathBox`は`IsReadOnly=True`のため通常操作では不正文字列を入力できず、値は既存設定値/`OpenFolderDialog`選択結果/空文字のいずれかに限られ、かつ使用時(`MainWindow.StartReading_Click`)で`Directory.Exists`による再検証があるため問題なしと確認。**観察点(据え置き、軽微)**: `BrowseFolder_Click`(19行目)が`OpenFolderDialog.InitialDirectory`設定時に既存値の空文字チェックのみで`Directory.Exists`を見ていないため、設定JSON手動編集等で存在しないパスが入っていた場合にダイアログの初期ディレクトリ指定が無視される可能性があるが、実害は極めて小さいと判断し見送り

### ErrorLogger.cs

状態: 済(2026-07-19、Codex相談、実バグ1件修正+観察点2件)

MainWindow各所（例外通知・error.log出力）・CharmEditWindow・DuplicateCheckWindow(今回のセッションで呼び出し追加)。全例外を`catch{}`で握りつぶす設計(ログ機構自体がクラッシュ原因にならないための防御)、マルチスレッド同時書き込みの排他制御なし(現在の呼び出し構造では実害なしとCodexとの相談で確認、`await`後もUIコンテキストへ戻るため)は問題なしと確認。**実バグ発見・修正**: `Log(category,message,ex)`(3引数版)が`ex.Message`のみを記録しスタックトレース・内部例外を記録しない設計だったため、デバッグ用途として不十分と判断し`ex.ToString()`(型・メッセージ・スタックトレース・内部例外を含む)に変更。**観察点(据え置き、配布形式検討中のため時期尚早と判断)**: (1)`LogFilePath`が`AppDomain.CurrentDomain.BaseDirectory`直下固定のため、Program Files配下等の書き込み不可な配置先にインストールされた場合、ログ自体が`catch{}`により無言で機能停止する。CLAUDE.mdに配布形式(インストーラーかポータブルか)が「検討中」と明記されており、配布形式が固まってから`LocalApplicationData`配下への変更等を検討するのが妥当。(2)`error.log`にサイズ上限・ローテーションが無く、スクショ一括処理で画像ごとに記録されるため長期運用では無制限に肥大化しうる。優先度低のため見送り

**追記(2026-08-03時点、配布形式は確定済み)**: 上記(1)の観察点は「配布形式検討中」を前提にしていたが、自己完結ポータブルzip配布が確定・v1.0.0〜v1.3.1で実際にリリース済みのため前提が古い。次回精査時に`LocalApplicationData`配下への変更を要否込みで再評価すること。

### GameSkillOrder.cs

状態: 済(2026-07-19、Codex相談、fail-fast化1件+回帰テスト追加)

skill-order.json・CharmEditWindow(ComboBox表示順)・SkillNameLoader(正規名との整合)。Pythonで実データ検証: 現行`resources/skill-order.json`(121件)は重複ゼロ、`skill-decoration-map.json`由来の正規名121種と完全一致(欠落・余剰なし)で実害はなかった。Codexとの相談で、他のハンドメンテJSONローダー(#8 SkillNameLoader・#12 RarityInference.ParseSkillGroups・#13 CharmTypeLoader)と同様の一貫性の観点でfail-fast化を推奨されたため対応。**変更**: (1)`JsonSerializer.Deserialize<List<string>>`の結果を`?? []`で握りつぶさず`InvalidOperationException`化、(2)null/空白/重複スキル名を検出時に例外化する`ParseJson`(internal)を新設。回帰テストとして`GameSkillOrderTests.cs`を新規作成し、`GameSkillOrder.Order`が`SkillNameLoader`由来の正規名集合と完全一致することを検証するテストも追加(手編集による将来の乖離をCIで検出可能に)。テスト追加に伴い`CharmChecker.Tests`から`CharmChecker.App`への`ProjectReference`と、`CharmChecker.App/AssemblyInfo.cs`への`InternalsVisibleTo("CharmChecker.Tests")`追加が必要だった(既存の`ThemeInfo`属性は保持)。**観察点(据え置き、実害なし)**: `Order`プロパティの実体は可変`List<string>`を`IReadOnlyList<string>`として公開しているため理論上は呼び出し元がキャストで変更可能だが、唯一の呼び出し元(`CharmEditWindow`)が即座に`ToList()`でコピーしているため実害なし

## 低優先（ボイラープレート）

### App.xaml.cs

状態: 済(2026-07-21、Codex相談、実バグ1件修正・実機検証済み)

MainWindow(`SystemThemeWatcher.Watch(this)`)。**2026-07-21の再精査(Codex相談)**: 多重起動防止(`Mutex`)・`StartupUri`削除の2026-07-21新規追加分を精査。**実バグ発見・修正(重要度高)**: 多重起動を検知した側(`createdNew==false`)は、既存Mutexを開いただけで所有権を取得していないにもかかわらず、`OnExit`で無条件に`ReleaseMutex()`を呼んでいた。`initiallyOwned:true`は「新規作成時のみ」所有権を付与するため、これは`ApplicationException`を投げ、後続の`Dispose()`/`base.OnExit(e)`が実行されない欠陥だった(Codexが`Mutex`のドキュメント仕様から指摘)。所有権を`_ownsInstanceLock`フラグで追跡し、実際に所有している場合のみ`ReleaseMutex()`を呼ぶよう修正。UI層のため自動テスト対象外だが、実機で二重起動を再現し(`Start-Process`で2プロセス起動)、修正後は「多重起動」ダイアログ表示→OK押下→例外なく正常終了、`error.log`に例外記録が無いことを確認済み(修正前のコードでは`ApplicationException`が発生していたはずの経路)。**その他の確認結果**: `Mutex`名(`MHWildsCharmChecker_`+MD5ハッシュ32文字)はWindowsの名前付きMutexの制約内で問題なし。`StartupUri`削除によるWPFリソース初期化順序への影響なし(`Application.Resources`は`InitializeComponent`で`OnStartup`より前に読み込まれるため)。`OnStartup`内で`MainWindow`生成中に例外が起きた場合もMutexハンドルはOS側で回収されるため他プロセスを永久に妨げることはない。**観察点(据え置き、Codex指摘)**: Mutex名に`Global\`が無いため名前空間はログオンセッション単位(同一Windowsセッション内のみ多重起動防止が効く)。ポータブルアプリの想定利用範囲では許容範囲と判断。前回(2026-07-12)は「ダーク固定は意図だがOS追従の機構自体は残っている」既知課題として将来のライトテーマ対応タスクの範疇で先送りしていたが、今回Codexとの相談で「現行仕様(ダーク固定)を前提にすると、これは将来課題ではなくOSライト環境で今すぐ到達可能な現行仕様との矛盾」と再評価。WPF-UI 4.3.0のXMLドキュメント(`Wpf.Ui.xml`)で`ApplicationThemeManager.Apply(ApplicationTheme, WindowBackdropType, bool)`という明示的固定テーマ適用APIの存在を確認。**実バグ発見・修正**: `App.xaml.cs`の`ApplicationThemeManager.ApplySystemTheme()`(OS追従、App.xamlの`Theme="Dark"`明示指定を実行時に上書きしうる)を`ApplicationThemeManager.Apply(ApplicationTheme.Dark)`(明示固定)に変更。`MainWindow.xaml.cs`の`SystemThemeWatcher.Watch(this)`(OS設定変更のたびにテーマ再適用、ドキュメント上「グローバル動作、Windowごとに変更不可」と明記)も削除し、未使用となった`using Wpf.Ui.Appearance;`も除去。ビルド成功・全137テスト成功を確認。実機起動もプロセス正常起動・`error.log`に例外記録なしを確認したが、UI外観の目視確認はスクリーンショット取得側の制約(Mica背景合成window領域がGDI `CopyFromScreen`で黒塗りになる現象、変更前コードでも同一再現を確認済みのため今回の変更由来ではないと切り分け済み)により実施できず。次回UI改修時に別の確認手段(例: `PrintWindow`API使用等)を検討

### AssemblyInfo.cs

状態: 済(2026-07-19、Codex相談、0件)

`app/CharmChecker.App/AssemblyInfo.cs`(WPFテンプレート標準ThemeInfo属性+#21の副産物として本日追加した`InternalsVisibleTo("CharmChecker.Tests")`)と`app/CharmChecker.Core/AssemblyInfo.cs`(#12で新規作成、`InternalsVisibleTo("CharmChecker.Tests")`のみ)の2ファイルが対象。両assemblyとも署名なし(strong-name未使用)のためPublicKey指定不要、`CharmChecker.Tests.csproj`はAssemblyName既定のまま(`InternalsVisibleTo`の対象名と一致)、`InternalsVisibleTo`と`ThemeInfo`の併記も型・用途が異なり無競合、とCodexとの相談で確認。**観察点**: `InternalsVisibleTo`はアセンブリ内の全`internal`メンバーをテストプロジェクトに公開する(個別メンバー限定は不可)が、通常のテスト設計として許容範囲と判断

## ドキュメント精査

### README.md

状態: 済(2026-07-19、Codex相談、全文書き直し+副産物で実バグ1件発見・修正)

前回(2026-07-12)発見の陳腐化(2026-06-14時点のC#移行前初期版のまま)を解消。CLAUDE.mdの9項目構成(冒頭英語一文・概要・動作環境/前提条件・現在の配布状況・使い方・既知の制約・CSV形式互換・ビルド方法・免責/帰属表示・ライセンス)で全文を書き直した。Codexとの相談で、配布状況の明記(バイナリ未配布・ソースビルド必須)・動作環境の利用者/開発者分離・データ保存場所(`charms.json`等が実行ファイルと同じフォルダ)の明記・OCR結果確認の促し、を追加観点として反映。ビルドコマンド(`dotnet build/run/test`のリポジトリ直下からの実行可否)・Windows実機検証環境(Windows 11のみ、Windows 10は未検証)を裏取りして記載。**副産物で実バグ発見・修正**: CSV形式の記述を実装(`CharmCsvConverter.ParseSlotValue`)と突き合わせたところ、CLAUDE.mdのCSV仕様(スロット値は0/1/2/3)と実装(0〜4を許容)が不一致であることが判明。#15で実装したバリデーション自体の上限が誤りだったため、`CharmCsvConverter.cs`(0〜3に修正)+回帰テスト2件で対応(詳細は#15のエントリに追記)

**追記(2026-07-20時点、CLAUDE.md記載のREADME運用方針変更、本ログ側は未反映)**: 2026-07-20に利用者向け優先の構成へ改訂（動作環境・前提条件は利用者向けのみを本文中段、ビルド関連は末尾「開発者向け情報」節に集約）。次回README.mdを精査する際はCLAUDE.mdの「README運用方針」節の最新構成9項目と本文を突き合わせること。

### CLAUDE.md

状態: 済(2026-07-19、Codex相談、記載漏れ4件+陳腐化2件を修正)

前回(2026-07-12)修正済みの武器スロットRARE8特殊ケースは維持されたまま、今回Codexとの相談で追加の記載漏れ・陳腐化を発見・修正。**記載漏れ(実装は正しいがドキュメント未記載)**: (1)「RARE推定ロジック」節に、`RarityInference.Infer`63-71行目の3つ目の特殊ケース(武器スロットなしでRARE7/8双方に一致する曖昧パターンはRARE8候補を除外しRARE7のみが残ればRARE7とする。テスト`Rare7_Group3_ArmorOnly`で裏付け)が未記載だったため追記。(2)「スロットアイコン判定ロジック」節に`SlotValidation.cs`の業務ルール(防具は降順ソート後1個目最大Lv3・2個目最大Lv1・3個目常に0、武器は最大1個・Lv1のみ、条件を満たさない検出値は破棄)が未記載だったため追記。**陳腐化(実装変更に伴いドキュメントが古いまま)**: (3)「スキル名の正規名と照合時の注意」節の「半角では照合に失敗する」という記載は、今回セッション#7で追加した`SkillNameNormalizer.ToFullWidthAscii`(半角ASCIIを照合前に全角へ正規化)により陳腐化していたため、現状に合わせて修正。(4)冒頭概要行と「C#移行の方針」節(「技術構成・実装方針」に節名変更)の「残タスクはREADME作成と配布形式の検討」は、#24でREADME.mdを全面改稿済みのため「公開に向けた必須の残タスクは配布形式の検討」に更新(未確認8スキルの調査等の任意タスクと区別するよう文言調整)。**その他**: 「README作成方針」節を「README運用方針」に改名し、実際に書いたREADME.mdの構成(概要見出し省略・配布状況節追加)に合わせて構成リストを更新。「RARE推定ロジック」節に8スキル問題(#12由来の宿題、未解決)をTODOとして明記し、正規名121種すべてがRARE推定可能なわけではない旨を可視化

## 解消済み事項

### #12/#21の宿題解消（2026-07-20）

`skill-decoration-map.json`にあって`skill-groups.json`に無い8スキル問題は、ユーザーが実機の「条件ソート→装備スキル」一覧（護石選択画面・装飾品選択画面）を書き出した2ファイルとの突き合わせで解決。7種（クライマー・ジャンプ鉄人・ハンター生活・昆虫標本の達人・緩衝・閃光強化・飛び込み）は装飾品専用スキルと確認、護石CSVに出現しないため`skill-groups.json`未収録でも実害なしと判断（対応不要）。残る1種（オトモへの采配）はユーザー確認により「防具スキルだが現時点では防具自体にのみ付く（護石・装飾品経由では付かない）」と判明し、`skill-decoration-map.json`の`extra_skills`から削除（正規名121種→120種）。連動して`resources/skill-order.json`・`app/CharmChecker.Tests/SkillNameLoaderTests.cs`（`Load_Returns121Skills`→`Load_Returns120Skills`、`Load_ContainsExtraSkills`からオトモへの采配を除外、否定側の回帰テスト`Load_DoesNotContainOtomoHaisai`を追加）・`resources/skill-name-checklist.md`・CLAUDE.mdを更新。全140テスト成功を再確認済み（`GameSkillOrderTests`は`SkillNameLoader`からの動的検証のためコード変更不要）。
